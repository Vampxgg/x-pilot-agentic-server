---
description: 代码修改后必须检查 README 和规则文件是否需要同步更新
alwaysApply: true
---

# 文档与规则同步更新规范

每次进行代码修改时，AI 和开发者必须检查以下文档是否需要同步更新。

## 检查清单

完成代码修改后，逐项评估：

1. **README.md** — 以下内容是否受影响：
   - 项目架构描述
   - 项目目录结构
   - API 端点列表
   - 内置工具列表
   - Quick Start 步骤
   - 关键能力说明

2. **`.cursor/rules/*.mdc`** — 是否需要：
   - 更新现有规则中的示例或约束
   - 新增规则以覆盖新引入的模式或约定

3. **Agent 定义文件** — 若修改了 `apps/` 下的 Agent：
   - 对应的 `agent.config.yaml` 是否与代码变更一致
   - `TOOLS.md` 中的工具列表是否与 `allowedTools` 匹配

## 触发条件

以下变更**必须**触发文档审查：

| 变更类型 | 需检查 |
|----------|--------|
| 新增/删除 API 路由 | README API 端点表 |
| 新增/删除内置工具 | README 工具列表 + 相关 Agent 的 TOOLS.md |
| 新增/删除 Agent | README 项目结构 + 可能的规则更新 |
| 修改核心架构（core/） | README 架构描述 + 开发规则 |
| 新增配置项 | README Configuration 部分 |
| 变更项目目录结构 | README Project Structure |

## 执行原则

- **宁可多检查，不可遗漏** — 不确定时就检查
- **原子更新** — 代码和文档在同一次修改中一起完成，不留"待补"
- **保持精简** — 只更新确实受影响的部分，不做无关修改
