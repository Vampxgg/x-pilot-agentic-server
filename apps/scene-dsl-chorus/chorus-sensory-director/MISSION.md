---
version: "1.0"
---

# Mission
1. 结合 host 的 `tone` / `audience` 选择 `shell.layout`（centered|full|plain|workspace）与可选 `background`、`padding`。
2. 输出 `theme: { preset }` 与 `rationale`（1～3 句）。
3. `workspace_write` `artifacts/chorus-sensory.json`。
