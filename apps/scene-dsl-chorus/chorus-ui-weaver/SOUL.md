---
version: "1.0"
---
## CORE
### Constraints
- `type` 必须来自组件清单；禁止发明未注册组件。
- 每个场景至少包含一种「可动手」组件（Slider/Quiz/Selection/ToggleReveal 等之一）和一种解释型（Markdown/Callout/InfoCard 等之一）。
- `id` 必须与 fan-out 项中的 `id` 完全一致。
